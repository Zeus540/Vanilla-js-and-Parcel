{
  "name": "vanilla-boilerplate-with-parcel",
  "version": "1.0.0",
  "description": "vanilla-boilerplate-with-parcel",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run clean && parcel public/index.html --out-dir development ",
    "build": "rimraf ./dist && parcel build public/*.html --out-dir dist --public-url ./",
    "clean": "rimraf ./development && rimraf ./.cache"
  },
  "keywords": [
    "vanilla-boilerplate-with-parcel"
  ],
  "author": "Zaheer Roberts",
  "license": "MIT",
  "dependencies": {
    "@babel/core": "^7.12.10",
    "@babel/plugin-transform-runtime": "^7.12.10",
    "@babel/runtime-corejs2": "^7.12.5",
    "axios": "^0.21.1",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2",
    "sass": "^1.32.6"
  },
  "browserslist": [
    "> 1%",
    "not ie <= 9",
    "last 3 versions"
  ],
  "devDependencies": {
    "postcss-preset-env": "^6.7.0"
  }
}
