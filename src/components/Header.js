
const Header = () => {
	const template = `
    <header >
      <h1 id="test">My Parcel App</h1>
      <p id="size" >This is a boilerplate for a simple vanilla JS workflow using the Parcel bundler.</p>
    </header>
  `;

	return template;
};

export default Header;
